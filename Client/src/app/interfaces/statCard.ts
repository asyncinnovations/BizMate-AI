// Interface definitions
export interface StatCardData {
  icon: React.ReactNode;
  iconBg: string;
  iconColor: string;
  badgeText: string;
  badgeBg: string;
  badgeColor: string;
  title: string;
  value: string;
  subtitle: string;
}